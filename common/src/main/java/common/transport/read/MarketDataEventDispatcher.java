package common.transport.read;

/**
 * Clients wishing to consume market data events must implement this interface. The transport module is not responsible
 * for any default implement whatsoever.
 */
public interface MarketDataEventDispatcher {
}
