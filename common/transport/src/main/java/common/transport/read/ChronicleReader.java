package common.transport.read;

final class ChronicleReader implements RawReader {

    @Override
    public void poll(WireDataHandler handler) {
        // TODO: call WireDataHandler after pulling data off wire
    }
}
