package common.core.message;

import java.nio.ByteBuffer;

/**
 * Packages together messages and also converts raw bytes into messages.
 */
public class MessageFactory {

    public static void formulate(ByteBuffer buffer, MutableMessage message) {

    }
}
